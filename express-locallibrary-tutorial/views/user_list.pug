extends layout

block content
  h1= title

  if user_list.length
    ul
      each user in user_list
        li
          a(href=user.url) #{user.name}
          |  (#{user.date_of_birth_formatted})
  else
    p There are no users.